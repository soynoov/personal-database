---
import searchIcon from '@tabler/icons/outline/search.svg?url';
import filtersIcon from '@tabler/icons/outline/adjustments-horizontal.svg?url';
import closeIcon from '@tabler/icons/outline/x.svg?url';
import gridIcon from '@tabler/icons/outline/layout-grid.svg?url';
import listIcon from '@tabler/icons/outline/list.svg?url';
import { PUBLIC_GAME_TAGS } from '../lib/game-tags';
import { GAME_MODES } from '../lib/game-modes';

interface Props {
  estados: string[];
  launchers: string[];
  plataformas: string[];
  totalGames: number;
  initialView?: 'cards' | 'table';
}

const { estados, launchers, plataformas, totalGames, initialView = 'cards' } = Astro.props;
---

<section class='panel panel-controls pc-controls mock-catalog-controls'>
  <div class='catalog-filters'>
    <div class='catalog-filters-top'>
      <div class='field field-search'>
        <label class='sr-only' for='search'>Buscar</label>
        <span class='field-search-icon' aria-hidden='true'>
          <img src={searchIcon} alt='' />
        </span>
        <input id='search' type='search' placeholder='Buscar juegos...' />
      </div>
      <div class='catalog-filter-actions'>
        <button
          class='catalog-filter-toggle'
          id='mobile-filter-toggle'
          type='button'
          aria-controls='mobile-extra-filters'
          aria-expanded='false'
        >
          <img src={filtersIcon} alt='' aria-hidden='true' />
          <span>Filtros</span>
          <span class='catalog-filter-count' id='catalog-filter-count' hidden>0</span>
        </button>
        <button class='button button-ghost button-clear' id='reset-filters' type='button' hidden>
          <img src={closeIcon} alt='' aria-hidden='true' />
          <span>Limpiar</span>
        </button>
      </div>
    </div>

    <button class='catalog-filter-backdrop' id='catalog-filter-backdrop' type='button' aria-label='Cerrar filtros' hidden></button>

    <div class='catalog-filters-selects' id='mobile-extra-filters'>
      <div class='catalog-filter-sheet-head'>
        <div>
          <span class='section-eyebrow'>Catálogo</span>
          <h2 id='catalog-filter-sheet-title'>Filtros</h2>
        </div>
        <button id='catalog-filter-close' type='button' aria-label='Cerrar filtros'>
          <img src={closeIcon} alt='' aria-hidden='true' />
        </button>
      </div>
      <div class='field cf-main'>
        <label for='estado'>Estado</label>
        <select id='estado'>
          <option value=''>Todos</option>
          {estados.map((estado) => <option value={estado}>{estado}</option>)}
        </select>
      </div>
      <div class='field cf-main'>
        <label for='launcher'>Launcher</label>
        <select id='launcher'>
          <option value=''>Todos</option>
          {launchers.map((launcher) => <option value={launcher}>{launcher}</option>)}
        </select>
      </div>
      <div class='field cf-main'>
        <label for='plataforma'>Plataforma</label>
        <select id='plataforma'>
          <option value=''>Todas</option>
          {plataformas.map((plataforma) => <option value={plataforma}>{plataforma}</option>)}
        </select>
      </div>
      <div class='field cf-extra cf-tag'>
        <label for='tag'>Etiqueta</label>
        <select id='tag'>
          <option value=''>Todas</option>
          {PUBLIC_GAME_TAGS.map((tag) => <option value={tag.value}>{tag.label}</option>)}
        </select>
      </div>
      <div class='field cf-main cf-sort'>
        <label for='sort'>Orden</label>
        <select id='sort'>
          <option value='titulo-asc'>A-Z</option>
          <option value='horas-desc' selected>Horas</option>
          <option value='lanzamiento-desc'>Reciente</option>
        </select>
      </div>
      <div class='field cf-extra cf-mode'>
        <label for='modo'>Modo</label>
        <select id='modo'>
          <option value=''>Todos</option>
          {GAME_MODES.map((mode) => <option value={mode.value}>{mode.label}</option>)}
        </select>
      </div>
      <div class='field cf-extra cf-price'>
        <label for='precio'>Precio</label>
        <select id='precio'>
          <option value=''>Todos</option>
          <option value='cheap'>Hasta 10 EUR</option>
          <option value='mid'>10 a 30 EUR</option>
          <option value='high'>Mas de 30 EUR</option>
          <option value='unknown'>Sin dato</option>
        </select>
      </div>
      <div class='field cf-extra cf-profitability'>
        <label for='rentabilidad'>Rentabilidad</label>
        <select id='rentabilidad'>
          <option value=''>Todas</option>
          <option value='amortized'>Amortizados</option>
          <option value='unamortized'>No amortizados</option>
          <option value='incomplete'>Datos incompletos</option>
        </select>
      </div>
      <div class='catalog-filter-sheet-actions'>
        <button class='catalog-filter-reset-mobile' id='reset-filters-mobile' type='button'>Limpiar</button>
        <button class='catalog-filter-apply' id='catalog-filter-apply' type='button'>
          Ver <strong data-results-count>{totalGames}</strong> juegos
        </button>
      </div>
    </div>
  </div>

  <div class='mobile-sort-bar'>
    <label for='mobile-sort'>Ordenar:</label>
    <select id='mobile-sort' aria-label='Ordenar catálogo'>
      <option value='titulo-asc'>A-Z</option>
      <option value='horas-desc' selected>Horas jugadas</option>
      <option value='lanzamiento-desc'>Más reciente</option>
    </select>
    <span class='mobile-results-count'><strong data-results-count>{totalGames}</strong> juegos</span>
  </div>

  <div class='mobile-tab-bar' id='mobile-tab-bar' role='group' aria-label='Filtros rápidos'>
    <button class='mobile-tab' type='button' data-quick-filter='' aria-pressed='true'>Todos</button>
    <button class='mobile-tab' type='button' data-quick-filter='jugando' aria-pressed='false'>
      <span class='mobile-tab-dot mobile-tab-dot-playing' aria-hidden='true'></span>Jugando
    </button>
    <button class='mobile-tab' type='button' data-quick-filter='terminado' aria-pressed='false'>
      <span class='mobile-tab-dot mobile-tab-dot-completed' aria-hidden='true'></span>Terminado
    </button>
    <button class='mobile-tab' type='button' data-quick-filter='wishlist' aria-pressed='false'>
      <span class='mobile-tab-dot mobile-tab-dot-wishlist' aria-hidden='true'></span>Wishlist
    </button>
  </div>

  <div class='active-filter-pills' id='active-filter-pills' hidden></div>

  <div class='panel-subtoolbar'>
    <div class='muted'>
      <strong id='results-count' data-results-count>{totalGames}</strong> juegos
    </div>
    <div class='view-toggle' role='group' aria-label='Modo de catalogo'>
      <button
        class:list={['view-toggle-button', initialView === 'cards' && 'is-active']}
        id='view-cards'
        type='button'
        data-view='cards'
        aria-pressed={initialView === 'cards' ? 'true' : 'false'}
        aria-label='Vista en cards'
        title='Vista en cards'
      >
        <img src={gridIcon} alt='' aria-hidden='true' />
        <span>Cards</span>
      </button>
      <button
        class:list={['view-toggle-button', initialView === 'table' && 'is-active']}
        id='view-table'
        type='button'
        data-view='table'
        aria-pressed={initialView === 'table' ? 'true' : 'false'}
        aria-label='Vista en tabla'
        title='Vista en tabla'
      >
        <img src={listIcon} alt='' aria-hidden='true' />
        <span>Tabla</span>
      </button>
    </div>
  </div>

  <div class='legend legend-app' role='group' aria-label='Filtros destacados'>
    <button class='legend-chip legend-chip-all' type='button' data-quick-filter=''>
      <span class='legend-dot'></span>Todos
    </button>
    <button class='legend-chip legend-chip-completed' type='button' data-quick-filter='terminado'>
      <span class='legend-dot'></span>Terminado
    </button>
    <button class='legend-chip legend-chip-playing' type='button' data-quick-filter='jugando'>
      <span class='legend-dot'></span>Jugando
    </button>
    <button class='legend-chip legend-chip-pending' type='button' data-quick-filter='pendiente'>
      <span class='legend-dot'></span>Pendiente
    </button>
    <button class='legend-chip legend-chip-free' type='button' data-quick-filter='free'>
      <span class='legend-dot'></span>Free to play
    </button>
    <button class='legend-chip legend-chip-amortized' type='button' data-quick-filter='amortized'>
      <span class='legend-dot'></span>Amortizado
    </button>
    <button class='legend-chip legend-chip-recurring' type='button' data-quick-filter='recurrente'>
      <span class='legend-dot'></span>Recurrentes
    </button>
    <button class='legend-chip legend-chip-early' type='button' data-quick-filter='early'>
      <span class='legend-dot'></span>Early Access
    </button>
  </div>
</section>
